import numpy as np 
M=int(input(' '))
N=int(input(' '))
A = np.zeros((N, M))
B = np.zeros((N, M))
C = np.zeros((N, M))
print(A)
print(B)
for i in range:
  for j in range:
    if A[i][j] > B[i][j]:
     C[i][j] = A[i][j]
    else:
      C[i][j] = B[i][j]
print(C)